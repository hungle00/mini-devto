<div class="py-3 border-bottom article-<%= article.id%>">
    <b>Title: <%= article.title %></b>
    <p>Description: <%= article.description %></p>
    <p>Tags count: <%= article.tag_list.count %></p>
    <div class="btn-group">
        <%= link_to 'Show', article, class: "btn btn-sm btn-outline-primary" %>
        <%= link_to "Edit", edit_article_path(article), class: "btn btn-sm btn-outline-primary" %>
        <%= link_to "Destroy", article, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-outline-danger" %>
    </div>
</div>
